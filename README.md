# RESTFul-API-in-Flask-With-JSON-Web-Token-Authentication-and-Flask-SQLAlchem


pip install -r /path/to/requirements.txt


run above command to install all dependicies

and use postman software for testing
